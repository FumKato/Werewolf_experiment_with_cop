if(Meteor.isClient){
	var operations = {
	
	};

	tmp_gm_context = new Context('tmp_gm', operations);
}

if(Meteor.isServer){
	var operations = {
	
	};

	tmp_gm_context = new Context('tmp_gm', operations);
};
